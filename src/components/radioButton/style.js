import { StyleSheet } from 'react-native';

import { colors } from '../../static/colors';

export const styles = StyleSheet.create({
  radioButtonText: { color: colors.DARKER_GREY },
  rowsContainer: { alignItems: 'center', flexDirection: 'row', justifyContent: 'flex-start' }
});
